# Reverse proxy: / -> frontend, /api/* -> API (prefix stripped)
# Hardcoded production domain.

pginbox.dev, www.pginbox.dev {
    handle_path /api/* {
        reverse_proxy api:3000
    }
    handle {
        reverse_proxy frontend:80
    }
}
