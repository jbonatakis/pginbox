# Reverse proxy: / -> frontend, /api/* -> API (prefix stripped)
# Canonical host is the bare domain.

www.pginbox.dev {
	redir https://pginbox.dev{uri} permanent
}

pginbox.dev {
	handle_path /api/* {
		reverse_proxy api:3000
	}

	handle {
		reverse_proxy frontend:80
	}
}
